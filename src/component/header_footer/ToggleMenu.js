import React from 'react';

const ToggleMenu = (props) => {
    return (
        <div className="drawerToggle" onClick={props.showToggle}>
        <div></div>
        <div></div>
        <div></div>
            
        </div>
    );
};

export default ToggleMenu;