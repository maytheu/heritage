import React from 'react';

const SongDetails = (props) => {
    console.log(props)
    return (
        <div className="container">
            song details here
        </div>
    );
};

export default SongDetails;